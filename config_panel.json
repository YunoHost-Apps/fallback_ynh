{
	"name": "Fallback configuration panel",
	"version": "0.1",
	"panel": [{
		"name": "Fallback, main server configuration",
		"id": "main_server",
		"sections": [{
			"name": "Encryption",
			"id": "encryption",
			"options": [{
				"name": "Do you want to encrypt your backups ?",
				"id": "encrypt",
				"type": "bool",
				"default": true
			},
			{
				"name": "Set the password for encryption",
				"help": "A password is needed if encryption is activated.",
				"id": "encryption_pwd",
				"type": "password",
				"optional": true
			}]
		},
		{
			"name": "ssh configuration",
			"id": "ssh_config",
			"options": [{
				"name": "Domain name for the fallback server ?",
				"help": {
					"en": "This server has to be up, for the ssh-keyscan command.",
					"fr": "Ce serveur doit être en route, pour la commande ssh-keyscan."
				},
				"id": "ssh_host",
				"type": "text"
			},
			{
				"name": "Port for the ssh connection to the fallback server ?",
				"id": "ssh_port",
				"type": "number",
				"default": 22
			}]
		}]
	},
	{
		"name": "Fallback, fallback server configuration",
		"id": "fallback_server",
		"sections": [{
			"name": "ssh configuration",
			"id": "ssh_config",
			"options": [{
				"name": "Public key from your main server ?",
				"id": "pub_key",
				"type": "text"
			}]
		},
		{
				"name": "Auto deployement",
				"id": "auto_deploy",
				"options": [{
					"name": "Auto detect a failure of the main server ?",
					"id": "auto_detect_failure",
					"type": "bool",
					"default" : true
				},
				{
					"name": "Delay before declaring an incident with the main server.",
					"id": "delay_before_incident",
					"help": "Value in minutes",
					"type": "number",
					"default" : "60",
					"optional": true
				},
				{
					"name": "Email address to contact in case of failure.",
					"id": "contact_mail",
					"type": "text",
					"optional": true
				},
				{
					"name": "Deploy automatically the fallback in case of failure ?",
					"id": "auto_deploy",
					"type": "bool",
					"default": false
			}]
		}]
	},
	{
		"name": "Fallback configuration",
		"id": "main",
		"sections": [{
			"name": "Global configuration",
			"id": "global_config",
			"options": [{
				"name": "Send HTML email to admin ?",
				"help": "Allow app scripts to send HTML mails instead of plain text.",
				"id": "email_type",
				"type": "bool",
				"default": true
			}]
		}]
	}
]
}
